export type ProductEvent="recording_started"|"recording_completed"|"wallet_connected"|"mint_preparation_started"|"mint_preparation_failed"|"mint_submitted"|"mint_confirmed"|"mint_failed";
export function track(event:ProductEvent,details:Record<string,string|number|boolean>={}){if(typeof window==="undefined"||(navigator as Navigator&{globalPrivacyControl?:boolean}).globalPrivacyControl)return;const body=JSON.stringify({event,details,path:location.pathname,at:new Date().toISOString()});try{if(navigator.sendBeacon){navigator.sendBeacon("/api/events",new Blob([body],{type:"application/json"}));return}void fetch("/api/events",{method:"POST",headers:{"content-type":"application/json"},body,keepalive:true})}catch{/* Analytics must never interrupt artwork creation. */}}
